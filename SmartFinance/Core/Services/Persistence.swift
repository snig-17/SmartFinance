import CoreData

struct PersistenceController {
    static let shared = PersistenceController()

    static var preview: PersistenceController = {
        let result = PersistenceController(inMemory: true)
        let viewContext = result.container.viewContext
        
        // Add sample data for previews
        let sampleTransaction = Transaction(context: viewContext)
        sampleTransaction.id = UUID()
        sampleTransaction.amount = NSDecimalNumber(value: 4.50)
        sampleTransaction.category = "Food & Dining"
        sampleTransaction.merchant = "Starbucks"
        sampleTransaction.date = Date()
        sampleTransaction.currency = "USD"
        sampleTransaction.paymentMethod = "Card"
        sampleTransaction.isRecurring = false
        
        do {
            try viewContext.save()
        } catch {
            let nsError = error as NSError
            fatalError("Unresolved error \(nsError), \(nsError.userInfo)")
        }
        return result
    }()

    let container: NSPersistentContainer

    init(inMemory: Bool = false) {
        container = NSPersistentContainer(name: "SmartFinance")
        if inMemory {
            container.persistentStoreDescriptions.first!.url = URL(fileURLWithPath: "/dev/null")
        }
        
        container.loadPersistentStores(completionHandler: { (storeDescription, error) in
            if let error = error as NSError? {
                fatalError("Unresolved error \(error), \(error.userInfo)")
            }
        })
        container.viewContext.automaticallyMergesChangesFromParent = true
    }
}
